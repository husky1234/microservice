server:
  port: 8761
spring:
  application:
    name: eureka-server-master
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8762/eureka/
